name: Keep Streamlit Alive

on:
  schedule:
    - cron: '0 0 * * *' # 매일 자정(UTC)에 실행 (한국 시간 기준 오전 9시)
  workflow_dispatch: # 버튼을 눌러서 수동으로도 실행할 수 있게 해주는 옵션

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Streamlit App
      run: curl -I https://gill-wa.streamlit.app/
